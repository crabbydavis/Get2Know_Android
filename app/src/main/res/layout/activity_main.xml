<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/activity_main"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="crabbman.get2know.UI.MainActivity"
    android:background="@color/new_gray">

    <TextView
        android:id="@+id/title"
        android:layout_width="280dp"
        android:layout_height="280dp"
        android:layout_centerHorizontal="true"
        android:paddingTop="40dp"
        android:background="@drawable/new_get2know_title"/>

    <Button
        android:id="@+id/playGameButton"
        android:layout_below="@+id/title"
        android:layout_width="180dp"
        android:layout_height="80dp"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true"
        android:text="Play Game"
        android:textSize="25dp"
        android:typeface="sans"
        android:background="@color/new_orange"
        />

    <Button
        android:id="@+id/freePlayButton"
        android:layout_below="@+id/playGameButton"
        android:layout_width="180dp"
        android:layout_height="80dp"
        android:layout_marginTop="30dp"
        android:layout_centerHorizontal="true"
        android:text="Free Play"
        android:textSize="25dp"
        android:typeface="sans"
        android:background="@color/new_orange"/>

    <ImageView
        android:id="@+id/informationAboutGame"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:src="@drawable/information_icon"
        android:padding="10dp"/>
</RelativeLayout>
